name: Test Workflow

on:
  push: 
    branches:
      - main 

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Welcome in screen 
        run: echo 'Welcome to my Workfllow'

